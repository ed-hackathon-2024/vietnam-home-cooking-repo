const prefix = '/live';

export const navbarConfig = {
  menuItems: [
    { label: 'Profile', href: `${prefix}/profile` },
    { label: 'Goals', href: `${prefix}/goals` },
    { label: 'Friends', href: `${prefix}/friends` },
    { label: 'Assistant', href: `${prefix}/chatbot` },
  ],
};
